# Beta v39 — もぐもぐチャレンジ失敗理由表示修正

## 修正
- `reason === "mogumogu"` の場合だけ「アリスが食べちゃった！」と表示。
- `reason === "dice"` の場合は「素数ではない！」と表示。
- 「素数ではないため、チャレンジ失敗です。」と明示。
- イベントpayloadの `reason` をモーダル表示処理へ渡すよう修正。

## 重要
ゲーム判定ロジック自体は変更していない。
「偶数だけもぐもぐ判定」「1・4・6は素数ではないため失敗」という既存仕様をUI上でも正しく表示するための修正。
