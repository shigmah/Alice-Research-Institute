# Beta v37 — もぐもぐモーダル改善

- 「次の一投を試す」をもぐもぐ結果モーダル内へ移動
- 成功時はモーダルを閉じず、そのまま次の一投を開始可能
- もぐもぐイベントは同一結果でもモーダルを再表示できるように修正
- 前回の「ログだけ更新されてモーダルが出ない」問題を解消
- 「出目はXです」という文字列を廃止し、実サイコロ画像をモーダル内に表示
- サイコロ画像が取得できない場合はテキストへフォールバック
- 終了時は「次の一投」を隠し、「閉じる」を再表示
